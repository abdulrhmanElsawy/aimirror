{
  "version": 2,
  "buildCommand": "cd client-tryon && npm install && npm run build",
  "outputDirectory": "client-tryon/dist",
  "installCommand": "cd server && npm install && cd ../client-tryon && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
